package com.second;

public class 数组元素的使用 {
    int a[][] = new int[6][8];
    int b[] = {1,2,3};
    //第一个索引范围则为0~5,第二个索引范围则为0~7
    public static void main(String args[]) {
        /*对一维数组来说，"数组名.length"的值就是数组中元素的个数
        *对二维数组来说，"数组名.length"的值则是数组中含有的一维元素的个数
         */
       // System.out.println(b.length);
        //二维数组也可这样直接定义：int a[][] = {{1},{1,1}}等等
    }
}
